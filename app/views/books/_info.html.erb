<%
  book = nil if book.nil?
%>
<% @categories = Category.find_by_id book.category_id  %> 
<% if book %>
    ♥ <%= book.bookmarks.length %>   💬 <%= book.reviews.length %>   ★<%= review_average(book.reviews) %> <br>
    <div class="category">
      <%= @categories.name %>
    </div>
<% end %>